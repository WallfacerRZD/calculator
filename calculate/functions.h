#ifndef  FUNCTIONS_H
#define FUNCTIONS_H

#include<string>
#include"BigData.h"

std::string TransforToPostFix(const std::string &s);
//int GetPostFixVal(const std::string &s);
BigData GetPostFixVal(const string &data);

#endif // ! FUNCTIONS_H